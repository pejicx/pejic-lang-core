# Dev container image for PejicLang Core
FROM mcr.microsoft.com/devcontainers/python:1-3.12

# Optional: install additional tools if needed
# RUN apt-get update && apt-get install -y --no-install-recommends <tools>

WORKDIR /workspace
